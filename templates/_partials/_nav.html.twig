<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">
			MaCopro
		</a>
		<div class="collapse navbar-collapse">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">

				{# ========================= #}
				{# COMPTABILITÉ              #}
				{# ========================= #}
					<li class="nav-item dropdown"> <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
						Comptabilité
					</a>
					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item" href="{{ path('app_balance') }}">
								Balance comptable
							</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_grand_livre') }}">
								Grand livre
							</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_journal') }}">
								Journal comptable
							</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_operation_index') }}">
								<i class="bi bi-journal-text"></i>
								Journal des opérations
							</a>
						</li>
						<li>
							<hr class="dropdown-divider">
						</li>
					</ul>
				</li>

				{# ========================= #}
				{# EXPLOITATION              #}
				{# ========================= #}
					<li class="nav-item dropdown"> <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
						Exploitation
					</a>
					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item" href="{{ path('app_budget_index') }}">
								Budgets
							</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_appel_fond_index') }}">
								<i class="bi bi-cash-stack"></i>
								Appels de fonds
							</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_facture_fournisseur_index') }}">
								<i class="bi bi-receipt"></i>
								Factures fournisseurs
							</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_paiement_index')}}">
								<i class="bi bi-cash-coin"></i>
								Paiements copropriétaires
							</a>
						</li>
					</ul>
				</li>

				{# ========================= #}
				{# TRÉSORERIE                #}
				{# ========================= #}
					<li class="nav-item dropdown"> <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
						Trésorerie
					</a>
					<ul
						class="dropdown-menu">
						{# Ajoutez vos liens de trésorerie ici si nécessaire #}
						<li>
							<a class="dropdown-item" href="#">Suivi bancaire</a>
						</li>
					</ul>
				</li>

				{# ========================= #}
				{# PARAMÉTRAGE               #}
				{# ========================= #}
					<li class="nav-item dropdown"> <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
						Paramétrage
					</a>
					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item" href="{{ path('app_copropriete_index') }}">
								Copropriétés
							</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_lot_index') }}">
								Lots
							</a>
						</li>
						<li>
							<hr class="dropdown-divider">
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_exercice_index') }}">
								Exercices
							</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_type_charge_index') }}">
								Types de charges
							</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_fournisseur_index') }}">
								Fournisseurs
							</a>
						</li>
					</ul>
				</li>
			</ul>
			<div class="d-flex gap-5">
				{# ========================= #}
				{# ACTIONS RAPIDES           #}
				{# ========================= #}

					<div class="d-flex gap-2"> <a href="{{ path('app_facture_fournisseur_new') }}" class="btn btn-light btn-sm">
						+ Facture fournisseur
					</a>
					<a href="{{ path('app_paiement_new') }}" class="btn btn-light btn-sm">
						+ Paiement copropriétaire
					</a>
					<a class="btn btn-light btn-sm" href="{{ path('app_coproprietaire_index') }}">
						Copropriétaires
					</a>
				</div>

				{# ========================= #}
				{# EXERCICE COURANT          #}
				{# ========================= #}
					{% if contexte.exercices is not empty %}
						<form method="post" action="{{ path('app_changer_exercice') }}" class="d-flex align-items-center gap-2"> <select name="exercice_id" class="form-select form-select-sm" onchange="this.form.submit();">
							{% for exercice in contexte.exercices %}
								<option value="{{ exercice.id }}" {{ contexte.exercice and exercice.id == contexte.exercice.id ? 'selected' : '' }}>
									{{ exercice.nom }}
								</option>
							{% endfor %}
						</select>
					</form>
				{% else %}
					<span class="navbar-text text-white">Aucun exercice</span>
				{% endif %}
			</div>
		</div>
	</div>
</nav>
