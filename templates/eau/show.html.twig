{% extends 'base.html.twig' %}

{% block title %}CompteurEau{% endblock %}

{% block body %}
    <h1>CompteurEau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compteur_eau.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ compteur_eau.reference }}</td>
            </tr>
            <tr>
                <th>IndexInitial</th>
                <td>{{ compteur_eau.indexInitial }}</td>
            </tr>
            <tr>
                <th>Actif</th>
                <td>{{ compteur_eau.actif ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_compteur_eau_index') }}">back to list</a>

    <a href="{{ path('app_compteur_eau_edit', {'id': compteur_eau.id}) }}">edit</a>

    {{ include('compteur_eau/_delete_form.html.twig') }}
{% endblock %}
