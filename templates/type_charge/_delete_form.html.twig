<form method="post" action="{{ path('app_type_charge_delete', {'id': type_charge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_charge.id) }}">
    <button class="btn">Delete</button>
</form>
