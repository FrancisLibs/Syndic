{% extends 'base.html.twig' %}

{% block title %}TypeCharge{% endblock %}

{% block body %}
    <h1>TypeCharge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_charge.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_charge.nom }}</td>
            </tr>
            <tr>
                <th>ModeRepartition</th>
                <td>{{ type_charge.modeRepartition.value }}</td>
            </tr>
            <tr>
                <th>Compte</th>
                <td>{{ type_charge.compte }}</td>
        </tbody>
    </table>

    <a href="{{ path('app_type_charge_index') }}">back to list</a>

    <a href="{{ path('app_type_charge_edit', {'id': type_charge.id}) }}">edit</a>

    {{ include('type_charge/_delete_form.html.twig') }}
{% endblock %}
