"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Copyright 2017 trivago N.V.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS-IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const he = __importStar(require("he"));
const n = __importStar(require("melody-types"));
const symbols_1 = require("../symbols");
const types_1 = require("../types");
const ElementInfo_1 = require("./ElementInfo");
const GenericMultiTagParser_1 = require("./GenericMultiTagParser");
const GenericTagParser_1 = require("./GenericTagParser");
const Types = __importStar(require("./TokenTypes"));
const util_1 = require("./util");
class Parser {
    constructor(tokenStream, options) {
        this[symbols_1.UNARY] = {};
        this[symbols_1.BINARY] = {};
        this[symbols_1.TAG] = {};
        this[symbols_1.TEST] = {};
        this.tokens = tokenStream;
        this.options = Object.assign({ ignoreComments: true, ignoreHtmlComments: true, ignoreDeclarations: true, decodeEntities: true, preserveSourceLiterally: false, allowUnknownTags: false, multiTags: {} }, options);
        // If there are custom multi tags, then we allow all custom tags
        if (Object.keys(this.options.multiTags).length > 0) {
            this.options.allowUnknownTags = true;
        }
    }
    applyExtension(ext) {
        if (ext.tags) {
            for (const tag of ext.tags) {
                this.addTag(tag);
            }
        }
        if (ext.unaryOperators) {
            for (const op of ext.unaryOperators) {
                this.addUnaryOperator(op);
            }
        }
        if (ext.binaryOperators) {
            for (const op of ext.binaryOperators) {
                this.addBinaryOperator(op);
            }
        }
        if (ext.tests) {
            for (const test of ext.tests) {
                this.addTest(test);
            }
        }
    }
    addUnaryOperator(op) {
        this[symbols_1.UNARY][op.text] = op;
        return this;
    }
    addBinaryOperator(op) {
        this[symbols_1.BINARY][op.text] = op;
        return this;
    }
    addTag(tag) {
        this[symbols_1.TAG][tag.name] = tag;
        return this;
    }
    addTest(test) {
        this[symbols_1.TEST][test.text] = test;
    }
    hasTest(test) {
        return !!this[symbols_1.TEST][test];
    }
    getTest(test) {
        return this[symbols_1.TEST][test];
    }
    isUnary(token) {
        return token.type === Types.OPERATOR && !!this[symbols_1.UNARY][token.text];
    }
    getBinaryOperator(token) {
        return token.type === Types.OPERATOR && this[symbols_1.BINARY][token.text];
    }
    parse(test = null) {
        const tokens = this.tokens;
        let p = (0, util_1.setStartFromToken)(new n.SequenceExpression(), tokens.la(0));
        while (!tokens.test(Types.EOF)) {
            const token = tokens.next();
            if (!p) {
                p = (0, util_1.setStartFromToken)(new n.SequenceExpression(), token);
            }
            if (test && test(tokens.la(0).text, token, tokens)) {
                (0, util_1.setEndFromToken)(p, token);
                return p;
            }
            switch (token.type) {
                case Types.EXPRESSION_START: {
                    const expression = this.matchExpression();
                    const statement = new n.PrintExpressionStatement(expression);
                    const endToken = tokens.expect(Types.EXPRESSION_END);
                    (0, util_1.setStartFromToken)(statement, token);
                    (0, util_1.setEndFromToken)(statement, endToken);
                    (0, util_1.setEndFromToken)(p, endToken);
                    statement.trimLeft = !!expression.trimLeft;
                    statement.trimRight = !!expression.trimRight;
                    p.add(statement);
                    break;
                }
                case Types.TAG_START:
                    p.add(this.matchTag());
                    break;
                case Types.TEXT: {
                    const textStringLiteral = (0, util_1.createNode)(n.StringLiteral, token, token.text);
                    const textTextStatement = (0, util_1.createNode)(n.PrintTextStatement, token, textStringLiteral);
                    p.add(textTextStatement);
                    break;
                }
                case Types.ENTITY: {
                    const entityStringLiteral = (0, util_1.createNode)(n.StringLiteral, token, !this.options.decodeEntities ||
                        this.options.preserveSourceLiterally
                        ? token.text
                        : he.decode(token.text));
                    const entityTextStatement = (0, util_1.createNode)(n.PrintTextStatement, token, entityStringLiteral);
                    p.add(entityTextStatement);
                    break;
                }
                case Types.ELEMENT_START:
                    p.add(this.matchElement());
                    break;
                case Types.DECLARATION_START: {
                    const declarationNode = this.matchDeclaration();
                    if (!this.options.ignoreDeclarations) {
                        p.add(declarationNode);
                    }
                    break;
                }
                case Types.COMMENT:
                    if (!this.options.ignoreComments) {
                        const stringLiteral = (0, util_1.createNode)(n.StringLiteral, token, token.text);
                        const twigComment = (0, util_1.createNode)(n.TwigComment, token, stringLiteral);
                        p.add(twigComment);
                    }
                    break;
                case Types.HTML_COMMENT:
                    if (!this.options.ignoreHtmlComments) {
                        const stringLiteral = (0, util_1.createNode)(n.StringLiteral, token, token.text);
                        const htmlComment = (0, util_1.createNode)(n.HtmlComment, token, stringLiteral);
                        p.add(htmlComment);
                    }
                    break;
            }
        }
        return p;
    }
    /**
     * e.g., <!DOCTYPE html>
     */
    matchDeclaration() {
        const tokens = this.tokens;
        const declarationStartToken = tokens.la(-1);
        let declarationType = null;
        let currentToken = null;
        if (!(declarationType = tokens.nextIf(Types.SYMBOL))) {
            this.error({
                title: 'Expected declaration start',
                pos: declarationStartToken.pos,
                advice: "After '<!', an unquoted symbol like DOCTYPE is expected",
            });
        }
        const declaration = new n.Declaration(declarationType.text);
        while ((currentToken = tokens.next())) {
            if (currentToken.type === Types.SYMBOL) {
                const symbol = (0, util_1.createNode)(n.Identifier, currentToken, currentToken.text);
                declaration.parts.push(symbol);
            }
            else if (currentToken.type === Types.STRING_START) {
                const stringToken = tokens.expect(Types.STRING);
                declaration.parts.push((0, util_1.createNode)(n.StringLiteral, stringToken, stringToken === null || stringToken === void 0 ? void 0 : stringToken.text));
                tokens.expect(Types.STRING_END);
            }
            else if (currentToken.type === Types.EXPRESSION_START) {
                const expression = this.matchExpression();
                declaration.parts.push((0, util_1.copyLoc)(new n.PrintExpressionStatement(expression), expression));
                tokens.expect(Types.EXPRESSION_END);
            }
            else if (currentToken.type === Types.ELEMENT_END) {
                break;
            }
            else {
                this.error({
                    title: 'Expected string, symbol, or expression',
                    pos: currentToken.pos,
                    advice: 'Only strings or symbols can be part of a declaration',
                });
            }
        }
        (0, util_1.setStartFromToken)(declaration, declarationStartToken);
        (0, util_1.setEndFromToken)(declaration, currentToken);
        return declaration;
    }
    /**
     * matchElement = '<' SYMBOL attributes* '/'? '>' (children)* '<' '/' SYMBOL '>'
     * attributes = SYMBOL '=' (matchExpression | matchString)
     *              | matchExpression
     */
    matchElement() {
        const tokens = this.tokens, elementNameToken = tokens.la(0), tagStartToken = tokens.la(-1);
        let elementName;
        if (!(elementName = tokens.nextIf(Types.SYMBOL))) {
            this.error({
                title: 'Expected element start',
                pos: elementNameToken.pos,
                advice: tokens.lat(0) === Types.SLASH
                    ? `Unexpected closing "${tokens.la(1).text}" tag. Seems like your DOM is out of control.`
                    : 'Expected an element to start',
            });
        }
        const element = new n.Element(elementName.text);
        this.matchAttributes(element, tokens);
        if (tokens.nextIf(Types.SLASH)) {
            tokens.expect(Types.ELEMENT_END);
            element.selfClosing = true;
        }
        else {
            tokens.expect(Types.ELEMENT_END);
            if (ElementInfo_1.voidElements[elementName.text]) {
                element.selfClosing = true;
            }
            else {
                element.children = this.parse(function (_, token, tokens) {
                    if (token.type === Types.ELEMENT_START &&
                        tokens.lat(0) === Types.SLASH) {
                        const name = tokens.la(1);
                        if (name.type === Types.SYMBOL &&
                            name.text === elementName.text) {
                            tokens.next(); // SLASH
                            tokens.next(); // elementName
                            tokens.expect(Types.ELEMENT_END);
                            return true;
                        }
                    }
                    return false;
                }).expressions;
            }
        }
        (0, util_1.setStartFromToken)(element, tagStartToken);
        (0, util_1.setEndFromToken)(element, tokens.la(-1));
        (0, util_1.setMarkFromToken)(element, 'elementNameLoc', elementNameToken);
        return element;
    }
    matchAttributes(element, tokens) {
        while (tokens.lat(0) !== Types.SLASH &&
            tokens.lat(0) !== Types.ELEMENT_END) {
            const key = tokens.nextIf(Types.SYMBOL);
            if (key) {
                const keyNode = new n.Identifier(key.text);
                (0, util_1.setStartFromToken)(keyNode, key);
                (0, util_1.setEndFromToken)(keyNode, key);
                // match an attribute
                if (tokens.nextIf(Types.ASSIGNMENT)) {
                    const start = tokens.expect(Types.STRING_START);
                    const nodes = [];
                    let canBeString = true, token;
                    while (!tokens.test(Types.STRING_END)) {
                        if (canBeString &&
                            (token = tokens.nextIf(Types.STRING))) {
                            nodes[nodes.length] = (0, util_1.createNode)(n.StringLiteral, token, token.text);
                            canBeString = false;
                        }
                        else if ((token = tokens.nextIf(Types.EXPRESSION_START))) {
                            nodes[nodes.length] = this.matchExpression();
                            tokens.expect(Types.EXPRESSION_END);
                            canBeString = true;
                        }
                        else {
                            break;
                        }
                    }
                    tokens.expect(Types.STRING_END);
                    if (!nodes.length) {
                        const node = (0, util_1.createNode)(n.StringLiteral, start, '');
                        nodes.push(node);
                    }
                    let expr = nodes[0];
                    for (let i = 1, len = nodes.length; i < len; i++) {
                        const { line, column } = expr.loc.start;
                        expr = new n.BinaryConcatExpression(expr, nodes[i]);
                        expr.loc.start.line = line;
                        expr.loc.start.column = column;
                        (0, util_1.copyEnd)(expr, expr.right);
                    }
                    // Distinguish between BinaryConcatExpression generated by
                    // this Parser (implicit before parsing), and those that the
                    // user wrote explicitly.
                    if (nodes.length > 1) {
                        expr.wasImplicitConcatenation = true;
                    }
                    const attr = new n.Attribute(keyNode, expr);
                    (0, util_1.copyStart)(attr, keyNode);
                    (0, util_1.copyEnd)(attr, expr);
                    element.attributes.push(attr);
                }
                else {
                    element.attributes.push((0, util_1.copyLoc)(new n.Attribute(keyNode), keyNode));
                }
            }
            else if (tokens.nextIf(Types.EXPRESSION_START)) {
                element.attributes.push(this.matchExpression());
                tokens.expect(Types.EXPRESSION_END);
            }
            else {
                this.error({
                    title: 'Invalid token',
                    pos: tokens.la(0).pos,
                    advice: 'A tag must consist of attributes or expressions. Twig Tags are not allowed.',
                });
            }
        }
    }
    error(options, metadata = {}) {
        this.tokens.error(options.title, options.pos, options.advice, 1, metadata);
    }
    getGenericParserFor(tagName) {
        if (this.options.multiTags[tagName]) {
            return (0, GenericMultiTagParser_1.createMultiTagParser)(tagName, this.options.multiTags[tagName]);
        }
        else {
            return GenericTagParser_1.GenericTagParser;
        }
    }
    matchTag() {
        const tokens = this.tokens;
        const tagStartToken = tokens.la(-1);
        const tag = tokens.expect(Types.SYMBOL);
        let parser = this[symbols_1.TAG][tag.text];
        let isUsingGenericParser = false;
        if (!parser) {
            if (this.options.allowUnknownTags) {
                parser = this.getGenericParserFor(tag.text);
                isUsingGenericParser = true;
            }
            else {
                tokens.error(`Unknown tag "${tag.text}"`, tag.pos, `Expected a known tag such as\n- ${Object.getOwnPropertyNames(this[symbols_1.TAG]).join('\n- ')}`, tag.length);
            }
        }
        const result = parser.parse(this, tag);
        const tagEndToken = tokens.la(-1);
        if (!isUsingGenericParser) {
            result.trimLeft = tagStartToken.text.endsWith('-');
            result.trimRight = tagEndToken.text.startsWith('-');
        }
        (0, util_1.setStartFromToken)(result, tagStartToken);
        (0, util_1.setEndFromToken)(result, tagEndToken);
        (0, util_1.setMarkFromToken)(result, 'tagNameLoc', tag);
        return result;
    }
    matchExpression(precedence = 0) {
        const tokens = this.tokens;
        const exprStartToken = tokens.la(0);
        let token;
        let op;
        let trimLeft = false;
        // Check for {{- (trim preceding whitespace)
        if (tokens.la(-1).type === Types.EXPRESSION_START &&
            tokens.la(-1).text.endsWith('-')) {
            trimLeft = true;
        }
        let expr = this.getPrimary();
        while ((token = tokens.la(0)) &&
            token.type !== Types.EOF &&
            (op = this.getBinaryOperator(token)) &&
            op.precedence >= precedence) {
            const opToken = tokens.next(); // consume the operator
            if (op.parse) {
                expr = op.parse(this, opToken, expr);
            }
            else {
                const expr1 = this.matchExpression(op.associativity === symbols_1.LEFT
                    ? op.precedence + 1
                    : op.precedence);
                expr = op.createNode(token, expr, expr1);
            }
            token = tokens.la(0);
        }
        let result = expr;
        if (precedence === 0) {
            (0, util_1.setEndFromToken)(expr, tokens.la(-1));
            result = this.matchConditionalExpression(expr);
            // Update the local token variable because the stream pointer already advanced.
            token = tokens.la(0);
        }
        // Check for -}} (trim following whitespace)
        if (token.type === Types.EXPRESSION_END && token.text.startsWith('-')) {
            result.trimRight = true;
        }
        if (trimLeft) {
            result.trimLeft = trimLeft;
        }
        const exprEndToken = tokens.la(-1);
        (0, util_1.setStartFromToken)(result, exprStartToken);
        (0, util_1.setEndFromToken)(result, exprEndToken);
        return result;
    }
    getPrimary() {
        const tokens = this.tokens;
        const token = tokens.la(0);
        const next_six_token = [
            tokens.la(0),
            tokens.la(1),
            tokens.la(2),
            tokens.la(3),
            tokens.la(4),
            tokens.la(5),
        ];
        const arrow_pos = next_six_token.findIndex((t) => t.type === Types.ARROW_TYPE);
        if (this.isUnary(token)) {
            const op = this[symbols_1.UNARY][token.text];
            tokens.next(); // consume operator
            const expr = this.matchExpression(op.precedence);
            return this.matchPostfixExpression(op.createNode(token, expr));
        }
        else if ((tokens.test(Types.LPAREN) && arrow_pos === 5) ||
            (tokens.test(Types.SYMBOL) && arrow_pos === 1)) {
            const args = tokens.test(Types.LPAREN)
                ? this.matchArguments()
                : [(0, util_1.createNode)(n.Identifier, tokens.la(0), tokens.next().text)];
            tokens.next(); // consume the arrow
            const body = this.matchExpression();
            return new types_1.ArrowFunctionExpression(args, body);
        }
        else if (tokens.test(Types.LPAREN)) {
            tokens.next(); // consume '('
            const expr = this.matchExpression();
            tokens.expect(Types.RPAREN);
            return this.matchPostfixExpression(expr);
        }
        return this.matchPrimaryExpression();
    }
    matchPrimaryExpression() {
        const tokens = this.tokens;
        const token = tokens.la(0);
        let node;
        switch (token.type) {
            case Types.NULL:
                node = (0, util_1.createNode)(n.NullLiteral, tokens.next());
                break;
            case Types.FALSE:
                node = (0, util_1.createNode)(n.BooleanLiteral, tokens.next(), false);
                break;
            case Types.TRUE:
                node = (0, util_1.createNode)(n.BooleanLiteral, tokens.next(), true);
                break;
            case Types.SYMBOL:
                tokens.next();
                if (tokens.test(Types.LPAREN)) {
                    // SYMBOL '(' arguments* ')'
                    node = new n.CallExpression((0, util_1.createNode)(n.Identifier, token, token.text), this.matchArguments());
                    (0, util_1.copyStart)(node, node.callee);
                    (0, util_1.setEndFromToken)(node, tokens.la(-1)); // ')'
                }
                else {
                    node = (0, util_1.createNode)(n.Identifier, token, token.text);
                }
                break;
            case Types.NUMBER:
                node = (0, util_1.createNode)(n.NumericLiteral, token, Number(tokens.next()));
                break;
            case Types.STRING_START:
                node = this.matchStringExpression();
                break;
            // potentially missing: OPERATOR type
            default:
                if (token.type === Types.LBRACE) {
                    node = this.matchArray();
                }
                else if (token.type === Types.LBRACKET) {
                    node = this.matchMap();
                }
                else {
                    this.error({
                        title: 'Unexpected token "' +
                            token.type +
                            '" of value "' +
                            token.text +
                            '"',
                        pos: token.pos,
                    }, {
                        errorType: 'UNEXPECTED_TOKEN',
                        tokenText: token.text,
                        tokenType: token.type,
                    });
                }
                break;
        }
        return this.matchPostfixExpression(node);
    }
    matchStringExpression() {
        let canBeString = true, token;
        const tokens = this.tokens;
        const nodes = [];
        const stringStart = tokens.expect(Types.STRING_START);
        while (!tokens.test(Types.STRING_END)) {
            if (canBeString && (token = tokens.nextIf(Types.STRING))) {
                nodes[nodes.length] = (0, util_1.createNode)(n.StringLiteral, token, token.text);
                canBeString = false;
            }
            else if ((token = tokens.nextIf(Types.INTERPOLATION_START))) {
                nodes[nodes.length] = this.matchExpression();
                tokens.expect(Types.INTERPOLATION_END);
                canBeString = true;
            }
            else {
                break;
            }
        }
        const stringEnd = tokens.expect(Types.STRING_END);
        if (!nodes.length) {
            return (0, util_1.setEndFromToken)((0, util_1.createNode)(n.StringLiteral, stringStart, ''), stringEnd);
        }
        let expr = nodes[0];
        for (let i = 1, len = nodes.length; i < len; i++) {
            const { line, column } = expr.loc.start;
            expr = new n.BinaryConcatExpression(expr, nodes[i]);
            expr.loc.start.line = line;
            expr.loc.start.column = column;
            (0, util_1.copyEnd)(expr, expr.right);
        }
        if (nodes.length > 1) {
            expr.wasImplicitConcatenation = true;
        }
        (0, util_1.setStartFromToken)(expr, stringStart);
        (0, util_1.setEndFromToken)(expr, stringEnd);
        return expr;
    }
    matchConditionalExpression(test) {
        const tokens = this.tokens;
        let condition = test, consequent, alternate;
        while (tokens.nextIf(Types.QUESTION_MARK)) {
            if (!tokens.nextIf(Types.COLON)) {
                consequent = this.matchExpression();
                if (tokens.nextIf(Types.COLON)) {
                    alternate = this.matchExpression();
                }
                else {
                    alternate = null;
                }
            }
            else {
                consequent = null;
                alternate = this.matchExpression();
            }
            const { line, column } = condition.loc.start;
            condition = new n.ConditionalExpression(condition, consequent, alternate);
            condition.loc.start = { line, column };
            (0, util_1.copyEnd)(condition, alternate || consequent);
        }
        return condition;
    }
    matchArray() {
        const tokens = this.tokens, array = new n.ArrayExpression(), start = tokens.expect(Types.LBRACE);
        (0, util_1.setStartFromToken)(array, start);
        while (!tokens.test(Types.RBRACE) && !tokens.test(Types.EOF)) {
            array.elements.push(this.matchExpression());
            if (!tokens.test(Types.RBRACE)) {
                tokens.expect(Types.COMMA);
                // support trailing commas
                if (tokens.test(Types.RBRACE)) {
                    break;
                }
            }
        }
        (0, util_1.setEndFromToken)(array, tokens.expect(Types.RBRACE));
        return array;
    }
    matchMap() {
        const tokens = this.tokens;
        const obj = Object.assign(new n.ObjectExpression(), {
            node_type: 'ObjectExpression',
        });
        const startToken = tokens.expect(Types.LBRACKET);
        let token;
        (0, util_1.setStartFromToken)(obj, startToken);
        while (!tokens.test(Types.RBRACKET) && !tokens.test(Types.EOF)) {
            let computed = false;
            let key;
            if (tokens.test(Types.STRING_START)) {
                key = this.matchStringExpression();
                if (!n.is(key, 'StringLiteral')) {
                    computed = true;
                }
            }
            else if ((token = tokens.nextIf(Types.SYMBOL))) {
                key = (0, util_1.createNode)(n.Identifier, token, token.text);
            }
            else if ((token = tokens.nextIf(Types.NUMBER))) {
                key = (0, util_1.createNode)(n.NumericLiteral, token, Number(token.text));
            }
            else if (tokens.test(Types.LPAREN)) {
                key = this.matchExpression();
                computed = true;
            }
            else {
                this.error({
                    title: 'Invalid map key',
                    pos: tokens.la(0).pos,
                    advice: 'Key must be a string, symbol or a number but was ' +
                        tokens.next(),
                });
            }
            tokens.expect(Types.COLON);
            const value = this.matchExpression();
            const prop = new n.ObjectProperty(key, value, computed);
            (0, util_1.copyStart)(prop, key);
            (0, util_1.copyEnd)(prop, value);
            obj.properties.push(prop);
            if (!tokens.test(Types.RBRACKET)) {
                tokens.expect(Types.COMMA);
                // support trailing comma
                if (tokens.test(Types.RBRACKET)) {
                    break;
                }
            }
        }
        (0, util_1.setEndFromToken)(obj, tokens.expect(Types.RBRACKET));
        return obj;
    }
    matchPostfixExpression(expr) {
        const tokens = this.tokens;
        let node = expr;
        while (!tokens.test(Types.EOF)) {
            if (tokens.test(Types.DOT) || tokens.test(Types.LBRACE)) {
                node = this.matchSubscriptExpression(node);
            }
            else if (tokens.test(Types.PIPE)) {
                tokens.next();
                node = this.matchFilterExpression(node);
            }
            else {
                break;
            }
        }
        return node;
    }
    matchSubscriptExpression(node) {
        const tokens = this.tokens, op = tokens.next();
        if (op.type === Types.DOT) {
            const token = tokens.next();
            let computed = false, property;
            if (token.type === Types.SYMBOL) {
                property = (0, util_1.createNode)(n.Identifier, token, token.text);
            }
            else if (token.type === Types.NUMBER) {
                property = (0, util_1.createNode)(n.NumericLiteral, token, Number(token.text));
                computed = true;
            }
            else {
                this.error({
                    title: 'Invalid token',
                    pos: token.pos,
                    advice: 'Expected number or symbol, found ' +
                        token +
                        ' instead',
                });
            }
            const memberExpr = new n.MemberExpression(node, property, computed);
            (0, util_1.copyStart)(memberExpr, node);
            (0, util_1.copyEnd)(memberExpr, property);
            if (tokens.test(Types.LPAREN)) {
                const callExpr = new n.CallExpression(memberExpr, this.matchArguments());
                (0, util_1.copyStart)(callExpr, memberExpr);
                (0, util_1.setEndFromToken)(callExpr, tokens.la(-1));
                return callExpr;
            }
            return memberExpr;
        }
        else {
            let arg, start;
            if (tokens.test(Types.COLON)) {
                // slice
                tokens.next();
                start = null;
            }
            else {
                arg = this.matchExpression();
                if (tokens.test(Types.COLON)) {
                    start = arg;
                    arg = null;
                    tokens.next();
                }
            }
            if (arg) {
                return (0, util_1.setEndFromToken)((0, util_1.copyStart)(new n.MemberExpression(node, arg, true), node), tokens.expect(Types.RBRACE));
            }
            else {
                // slice
                const result = new n.SliceExpression(node, start, tokens.test(Types.RBRACE) ? null : this.matchExpression());
                (0, util_1.copyStart)(result, node);
                (0, util_1.setEndFromToken)(result, tokens.expect(Types.RBRACE));
                return result;
            }
        }
    }
    matchFilterExpression(node) {
        const tokens = this.tokens;
        let target = node;
        while (!tokens.test(Types.EOF)) {
            const token = tokens.expect(Types.SYMBOL);
            const name = (0, util_1.createNode)(n.Identifier, token, token.text);
            const args = tokens.test(Types.LPAREN)
                ? this.matchArguments()
                : [];
            const newTarget = new n.FilterExpression(target, name, args);
            (0, util_1.copyStart)(newTarget, target);
            if (newTarget.arguments.length) {
                (0, util_1.copyEnd)(newTarget, newTarget.arguments[newTarget.arguments.length - 1]);
            }
            else {
                (0, util_1.copyEnd)(newTarget, target);
            }
            target = newTarget;
            if (!tokens.test(Types.PIPE) || tokens.test(Types.EOF)) {
                break;
            }
            tokens.next(); // consume '|'
        }
        return target;
    }
    matchArguments() {
        const tokens = this.tokens;
        const args = [];
        tokens.expect(Types.LPAREN);
        while (!tokens.test(Types.RPAREN) && !tokens.test(Types.EOF)) {
            if (tokens.test(Types.SYMBOL) &&
                tokens.lat(1) === Types.ASSIGNMENT) {
                const name = tokens.next();
                tokens.next();
                const value = this.matchExpression();
                const arg = new n.NamedArgumentExpression((0, util_1.createNode)(n.Identifier, name, name.text), value);
                (0, util_1.copyEnd)(arg, value);
                args.push(arg);
            }
            else {
                args.push(this.matchExpression());
            }
            if (!tokens.test(Types.COMMA)) {
                tokens.expect(Types.RPAREN);
                return args;
            }
            tokens.expect(Types.COMMA);
        }
        tokens.expect(Types.RPAREN);
        return args;
    }
}
exports.default = Parser;
