"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printObjectExpression = void 0;
const n = __importStar(require("melody-types"));
const prettier_1 = __importDefault(require("prettier"));
const util_1 = __importDefault(require("../util"));
const PublicFunctions_1 = require("../util/PublicFunctions");
const PublicSymbols_1 = require("../util/PublicSymbols");
const { group, line, indent, join } = prettier_1.default.doc.builders;
const { EXPRESSION_NEEDED, wrapExpressionIfNeeded } = util_1.default;
const printObjectExpression = (node, path, print, options) => {
    if (node.properties.length === 0) {
        return '{}';
    }
    node[EXPRESSION_NEEDED] = false;
    const contains_invalid_Key = node.properties.some((prop) => {
        const value = n.Node.isStringLiteral(prop.key)
            ? prop.key.value
            : prop.key.name;
        return !(0, PublicFunctions_1.isValidIdentifierName)(value);
    });
    if (options.quoteProps === 'consistent' && contains_invalid_Key) {
        node.properties = node.properties.map((p) => {
            if (p.key.node_type === 'Identifier') {
                p.key = Object.assign(new n.StringLiteral(p.key.name), {
                    loc: p.key.loc,
                    node_type: 'StringLiteral',
                });
            }
            p[PublicSymbols_1.STRING_NEEDS_QUOTES] = true;
            return p;
        });
    }
    const mappedElements = path.map(print, 'properties');
    const indentedContent = [line, join([',', line], mappedElements)];
    const parts = ['{', indent(indentedContent), line, '}'];
    wrapExpressionIfNeeded(path, parts, node);
    return group(parts);
};
exports.printObjectExpression = printObjectExpression;
