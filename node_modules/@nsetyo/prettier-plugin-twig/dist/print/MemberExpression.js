"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printMemberExpression = void 0;
const prettier_1 = __importDefault(require("prettier"));
const util_1 = __importDefault(require("../util"));
const { EXPRESSION_NEEDED, STRING_NEEDS_QUOTES, wrapExpressionIfNeeded } = util_1.default;
const { group } = prettier_1.default.doc.builders;
const printMemberExpression = (node, path, print) => {
    node[EXPRESSION_NEEDED] = false;
    node[STRING_NEEDS_QUOTES] = true;
    const parts = [path.call(print, 'object')];
    parts.push(node.computed ? '[' : '.');
    parts.push(path.call(print, 'property'));
    if (node.computed) {
        parts.push(']');
    }
    wrapExpressionIfNeeded(path, parts, node);
    return group(parts);
};
exports.printMemberExpression = printMemberExpression;
