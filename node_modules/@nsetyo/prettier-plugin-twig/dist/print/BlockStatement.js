"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printBlockStatement = void 0;
const melody_types_1 = require("melody-types");
const prettier_1 = __importDefault(require("prettier"));
const util_1 = __importDefault(require("../util"));
const { hardline, group } = prettier_1.default.doc.builders;
const { EXPRESSION_NEEDED, printChildBlock } = util_1.default;
const printBlockStatement = (node, path, print, options) => {
    node[EXPRESSION_NEEDED] = false;
    const hasChildren = Array.isArray(node.body);
    const printEndblockName = options.twigOutputEndblockName === true;
    if (hasChildren) {
        const blockName = path.call(print, 'name');
        const opener = [
            node.trimLeft ? '{%-' : '{%',
            ' block ',
            blockName,
            node.trimRightBlock ? ' -%}' : ' %}',
        ];
        const parts = [opener];
        if (node.body.length > 0) {
            const indentedBody = printChildBlock(node, path, print, 'body');
            parts.push(indentedBody);
        }
        parts.push(hardline);
        parts.push(node.trimLeftEndblock ? '{%-' : '{%', ' endblock', printEndblockName ? [' ', blockName] : '', node.trimRight ? ' -%}' : ' %}');
        const result = group(parts);
        return result;
    }
    else if (melody_types_1.Node.isPrintExpressionStatement(node.body)) {
        const parts = [
            node.trimLeft ? '{%-' : '{%',
            ' block ',
            path.call(print, 'name'),
            ' ',
            path.call(print, 'body', 'value'),
            node.trimRight ? ' -%}' : ' %}',
        ];
        return parts;
    }
};
exports.printBlockStatement = printBlockStatement;
