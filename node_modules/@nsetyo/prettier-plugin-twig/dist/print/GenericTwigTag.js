"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printGenericTwigTag = void 0;
const melody_types_1 = require("melody-types");
const prettier_1 = __importDefault(require("prettier"));
const util_1 = __importDefault(require("../util"));
const { hardline } = prettier_1.default.doc.builders;
const { STRING_NEEDS_QUOTES, indentWithHardline, printSingleTwigTag, isEmptySequence, } = util_1.default;
const printGenericTwigTag = (node, path, print) => {
    node[STRING_NEEDS_QUOTES] = true;
    const openingTag = printSingleTwigTag(node, path, print);
    const parts = [openingTag];
    const printedSections = path.map(print, 'sections');
    node.sections.forEach((section, i) => {
        if (melody_types_1.Node.isGenericTwigTag(section)) {
            parts.push([hardline, printedSections[i]]);
        }
        else {
            if (!isEmptySequence(section)) {
                // Indent
                parts.push(indentWithHardline(printedSections[i]));
            }
        }
    });
    return parts;
};
exports.printGenericTwigTag = printGenericTwigTag;
