"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printSetStatement = void 0;
const melody_types_1 = require("melody-types");
const prettier_1 = __importDefault(require("prettier"));
const util_1 = __importDefault(require("../util"));
const { group, line, hardline } = prettier_1.default.doc.builders;
const { printChildBlock, isNotExpression, STRING_NEEDS_QUOTES, GROUP_TOP_LEVEL_LOGICAL, } = util_1.default;
const shouldAvoidBreakBeforeClosing = (valueNode) => melody_types_1.Node.isObjectExpression(valueNode) ||
    isNotExpression(valueNode) ||
    melody_types_1.Node.isArrayExpression(valueNode);
const buildSetStatement = (node, path, print, assignmentIndex) => {
    const varDeclaration = node.assignments[assignmentIndex];
    varDeclaration[GROUP_TOP_LEVEL_LOGICAL] = false;
    const avoidBreakBeforeClosing = shouldAvoidBreakBeforeClosing(varDeclaration.value);
    return group([
        node.trimLeft ? '{%-' : '{%',
        ' set ',
        path.call(print, 'assignments', assignmentIndex),
        avoidBreakBeforeClosing ? ' ' : line,
        node.trimRight ? '-%}' : '%}',
    ]);
};
const isEmbracingSet = (node) => {
    return (Array.isArray(node.assignments) &&
        node.assignments.length === 1 &&
        Array.isArray(node.assignments[0].value));
};
const printRegularSet = (node, path, print) => {
    const parts = [];
    const hasAssignments = Array.isArray(node.assignments) && node.assignments.length > 0;
    if (hasAssignments) {
        node.assignments.forEach((_, index) => {
            if (parts.length > 0) {
                parts.push(hardline);
            }
            parts.push(buildSetStatement(node, path, print, index));
        });
    }
    return parts;
};
const printEmbracingSet = (node, path, print) => {
    const parts = [
        node.trimLeft ? '{%-' : '{%',
        ' set ',
        path.call(print, 'assignments', '0', 'name'),
        node.trimRightSet ? ' -%}' : ' %}',
    ];
    node[STRING_NEEDS_QUOTES] = false;
    const printedContents = printChildBlock(node, path, print, 'assignments', '0', 'value');
    // const printedContents = path.map(print, "assignments", "0", "value");
    parts.push(printedContents);
    parts.push(hardline, node.trimLeftEndset ? '{%-' : '{%', ' endset ', node.trimRight ? '-%}' : '%}');
    return parts;
};
const printSetStatement = (node, path, print) => {
    node[STRING_NEEDS_QUOTES] = true;
    if (isEmbracingSet(node)) {
        return printEmbracingSet(node, path, print);
    }
    return printRegularSet(node, path, print);
};
exports.printSetStatement = printSetStatement;
