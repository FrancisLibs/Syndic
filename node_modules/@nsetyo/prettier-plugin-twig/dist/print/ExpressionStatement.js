"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printExpressionStatement = void 0;
const melody_types_1 = require("melody-types");
const prettier_1 = __importDefault(require("prettier"));
const util_1 = __importDefault(require("../util"));
const { EXPRESSION_NEEDED, STRING_NEEDS_QUOTES, isContractableNodeType } = util_1.default;
const { group, indent, line } = prettier_1.default.doc.builders;
const printExpressionStatement = (node, path, print) => {
    node[EXPRESSION_NEEDED] = false;
    node[STRING_NEEDS_QUOTES] = true;
    const opener = node.trimLeft ? '{{-' : '{{';
    const closing = node.trimRight ? '-}}' : '}}';
    const shouldContractValue = isContractableNodeType(node.value) &&
        !melody_types_1.Node.isObjectExpression(node.value);
    const padding = shouldContractValue ? ' ' : line;
    const printedValue = [padding, path.call(print, 'value')];
    const value = shouldContractValue ? printedValue : indent(printedValue);
    return group([opener, value, padding, closing]);
};
exports.printExpressionStatement = printExpressionStatement;
