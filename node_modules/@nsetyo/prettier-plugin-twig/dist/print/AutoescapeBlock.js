"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printAutoescapeBlock = void 0;
const prettier_1 = __importDefault(require("prettier"));
const util_1 = __importDefault(require("../util"));
const { hardline } = prettier_1.default.doc.builders;
const { printChildBlock, quoteChar } = util_1.default;
const createOpener = (node, options) => {
    return [
        node.trimLeft ? '{%-' : '{%',
        ' autoescape ',
        quoteChar(options),
        node.escapeType || 'html',
        quoteChar(options),
        ' ',
        node.trimRightAutoescape ? '-%}' : '%}',
    ];
};
const printAutoescapeBlock = (node, path, print, options) => {
    const parts = [createOpener(node, options)];
    parts.push(printChildBlock(node, path, print, 'expressions'));
    parts.push(hardline, node.trimLeftEndautoescape ? '{%-' : '{%', ' endautoescape ', node.trimRight ? '-%}' : '%}');
    return parts;
};
exports.printAutoescapeBlock = printAutoescapeBlock;
