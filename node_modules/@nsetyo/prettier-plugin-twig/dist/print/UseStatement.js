"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printUseStatement = void 0;
const prettier_1 = __importDefault(require("prettier"));
const { group, indent, join, line } = prettier_1.default.doc.builders;
const printUseStatement = (node, path, print) => {
    const docs = [
        node.trimLeft ? '{%-' : '{%',
        ' use "',
        path.call(print, 'source'),
        '"',
    ];
    const hasAliases = node.aliases && node.aliases.length > 0;
    if (hasAliases) {
        docs.push(' with');
        const mappedAliases = path.map(print, 'aliases');
        docs.push(indent([line, join([',', line], mappedAliases)]));
        docs.push(line);
    }
    else {
        docs.push(' ');
    }
    docs.push(node.trimRight ? '-%}' : '%}');
    return group(docs);
};
exports.printUseStatement = printUseStatement;
