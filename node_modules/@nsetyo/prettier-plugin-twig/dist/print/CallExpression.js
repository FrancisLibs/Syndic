"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printCallExpression = void 0;
const melody_types_1 = require("melody-types");
const prettier_1 = __importDefault(require("prettier"));
const util_1 = __importDefault(require("../util"));
const { group, softline, line, indent, join } = prettier_1.default.doc.builders;
const { EXPRESSION_NEEDED, STRING_NEEDS_QUOTES, wrapExpressionIfNeeded } = util_1.default;
const printCallExpression = (node, path, print) => {
    node[EXPRESSION_NEEDED] = false;
    node[STRING_NEEDS_QUOTES] = true;
    const mappedArguments = path.map(print, 'arguments');
    const parts = [path.call(print, 'callee'), '('];
    if (node.arguments.length === 0) {
        parts.push(')');
    }
    else if (node.arguments.length === 1 &&
        melody_types_1.Node.isObjectExpression(node.arguments[0])) {
        // Optimization: No line break between "(" and "{" if
        // there is exactly one object parameter
        parts.push(mappedArguments[0], ')');
    }
    else {
        parts.push(indent([softline, join([',', line], mappedArguments)]), softline, ')');
    }
    wrapExpressionIfNeeded(path, parts, node);
    return group(parts);
};
exports.printCallExpression = printCallExpression;
