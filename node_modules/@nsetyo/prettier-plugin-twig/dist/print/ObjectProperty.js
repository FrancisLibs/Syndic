"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printObjectProperty = void 0;
const melody_types_1 = require("melody-types");
const util_1 = __importDefault(require("../util"));
const { isValidIdentifierName, STRING_NEEDS_QUOTES } = util_1.default;
const printObjectProperty = (node, path, print, options) => {
    node[STRING_NEEDS_QUOTES] =
        node[STRING_NEEDS_QUOTES] ||
            (!node.computed &&
                melody_types_1.Node.isStringLiteral(node.key) &&
                !isValidIdentifierName(node.key.value));
    const shouldPrintKeyAsString = node.key.wasImplicitConcatenation;
    const needsParentheses = node.computed && !shouldPrintKeyAsString;
    const parts = [];
    if (needsParentheses) {
        parts.push('(');
    }
    parts.push(path.call(print, 'key'));
    if (needsParentheses) {
        parts.push(')');
    }
    parts.push(': ');
    node[STRING_NEEDS_QUOTES] = true;
    parts.push(path.call(print, 'value'));
    return parts;
};
exports.printObjectProperty = printObjectProperty;
