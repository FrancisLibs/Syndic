"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.printHtmlComment = void 0;
const util_1 = __importDefault(require("../util"));
const { stripHtmlCommentChars, normalizeHtmlComment, countNewlines } = util_1.default;
const printHtmlComment = (node, path, print) => {
    const commentText = stripHtmlCommentChars(node.value.value || '');
    const numNewlines = countNewlines(commentText);
    if (numNewlines === 0) {
        return normalizeHtmlComment(commentText);
    }
    return ['<!-- ', commentText, ' -->'];
};
exports.printHtmlComment = printHtmlComment;
